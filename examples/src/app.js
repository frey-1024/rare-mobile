import React from 'react';
import ReactDOM from 'react-dom';
import 'rare-mobile/style/component.css';// eslint-disable-line
import Block from 'rare-mobile/src'; // eslint-disable-line

ReactDOM.render(
  <div className="component-template">
    <Block />
  </div>,
  document.getElementById('example'),
);
